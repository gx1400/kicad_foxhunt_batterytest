# Firmware (reserved)

Placeholder — no firmware exists yet. This will become a PlatformIO project
(`platformio.ini`, `src/`, `include/`, `lib/`) once firmware work starts.
Design docs for it go in [`_docs/firmware/`](../_docs/firmware/README.md).
